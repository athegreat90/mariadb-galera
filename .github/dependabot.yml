version: 2
updates:
  # No `docker` ecosystem on purpose: base images are pinned per MariaDB series in
  # .github/base-images.json and kept current by .github/workflows/base-image-watch.yml.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "chore(actions)"
